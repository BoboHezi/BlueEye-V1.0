<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <!--ToolBar颜色-->
        <item name="colorPrimary">@color/colorPrimary</item>
        <!--状态栏颜色-->
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <!--ToolBar字体颜色-->
        <item name="colorAccent">@color/colorAccent</item>
        <item name="windowNoTitle">true</item>
        <!--导航栏颜色-->
        <!--<item name="android:navigationBarColor">@color/navigationBarColor</item>-->
    </style>

    <!--图片/视频，播放Dialog动画-->
    <style name="animation_image_dialog">
        <item name="android:windowEnterAnimation">@anim/anim_dialog_popup_enter</item>
        <item name="android:windowExitAnimation">@anim/anim_dialog_popup_exit</item>
    </style>

    <!--更多操作Dialog动画-->
    <style name="animation_dialog_action">
        <item name="android:windowEnterAnimation">@anim/anim_action_enter</item>
        <item name="android:windowExitAnimation">@anim/anim_action_exit</item>
    </style>

    <!--删除Dialog动画-->
    <style name="animation_dialog_delete">
        <item name="android:windowEnterAnimation">@anim/anim_delete_enter</item>
        <item name="android:windowExitAnimation">@anim/anim_delete_exit</item>
    </style>

    <!--图片/视频 查看器风格-->
    <style name="style_image_dialog" parent="@android:style/Theme.Dialog">
        <item name="android:windowFrame">@null</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/white</item>
        <item name="android:backgroundDimEnabled">false</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowTranslucentStatus">true</item>
    </style>

    <!--更多操作Dialog风格-->
    <style name="style_dialog_action" parent="@android:style/Theme.Dialog">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowBackground">@android:color/white</item>
    </style>

    <!--分享平台选择Dialog-->
    <style name="dialog_share_platform" parent="@android:style/Theme.Dialog">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowBackground">@android:color/white</item>
    </style>

    <!--分享平台动画-->
    <style name="animation_share_dialog">
        <item name="android:windowEnterAnimation">@anim/share_platform_enter</item>
        <item name="android:windowExitAnimation">@anim/share_platform_exit</item>
    </style>
</resources>
